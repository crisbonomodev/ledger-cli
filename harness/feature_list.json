{
  "project": "ledger-cli",
  "last_updated": "2026-09-13",
  "rules": {
    "single_active_feature": true,
    "passing_requires_evidence": true,
    "do_not_skip_verification": true
  },
  "status_legend": {
    "not_started": "Work has not begun.",
    "in_progress": "The feature is the current active task.",
    "blocked": "Work cannot continue until a documented blocker is resolved.",
    "passing": "Required verification has passed and evidence is recorded."
  },
  "features": [
    {
      "id": "ledger-001",
      "priority": 1,
      "area": "ledger",
      "title": "View an account's history",
      "user_visible_behavior": "A user runs `history <account>` and sees every transaction for that account in chronological order, with the running balance after each one.",
      "status": "passing",
      "verification": [
        "Record 3 transactions on the same account with different dates.",
        "Run `history <account>`.",
        "Verify the chronological order and that the running balance on each line is correct."
      ],
      "evidence": [
        "npx vitest run src/ledger.test.ts -> 6 passed (6), including 'returns an account history in chronological order with a running balance' and 'excludes transactions belonging to other accounts' (src/ledger.test.ts:45-68)",
        "npm run check && npm test -> tsc --noEmit clean, 6/6 tests pass",
        "Manual CLI run in /tmp scratch dir: seeded ledger.json with 3 cash transactions appended out of date order (2026-01-05, 2026-01-01, 2026-01-03) plus 1 unrelated 'savings' transaction, then `node dist/index.js history cash` printed them sorted 01-01 -> 01-03 -> 01-05 with running balances 100, 150, 130 and correctly excluded the savings entry"
      ],
      "notes": "Ledger.history(account) added in src/ledger.ts; wired to `history <account>` CLI command in src/index.ts. Pre-existing bug noted (not in scope): the `record` CLI command always stamps today's date and does not parse 'credit'/'debit' argument strings into TransactionType — it only accepts the raw numeric enum value. Left unfixed since it predates this feature and isn't part of ledger-001's required behavior."
    },
    {
      "id": "ledger-002",
      "priority": 2,
      "area": "ledger",
      "title": "Void a transaction without deleting it",
      "user_visible_behavior": "A user runs `void <id>` and a reversing transaction is appended (credit/debit flipped); the original transaction still exists in the history.",
      "status": "passing",
      "verification": [
        "Record a transaction and note its id.",
        "Run `void <id>`.",
        "Verify the balance returns to its value from before the original transaction.",
        "Verify the original transaction still appears in `history` — it was not deleted."
      ],
      "evidence": [
        "Evaluator re-ran npm run check && npm test independently (clean tree, no changes) -> tsc --noEmit clean, 9/9 tests pass (matches implementer's claim).",
        "Evaluator confirmed rm -rf dist && ./init.sh completes end-to-end: npm install, npm run check && npm test (9/9 pass), npm run build -- all succeed from a clean dist/.",
        "Evaluator read src/ledger.test.ts:68-102 and confirmed the 3 required tests genuinely test the contracted behaviors: (a) reversal has flipped type (DEBIT vs original CREDIT), same amount, voidsId === original.id; (b) balanceOf('cash') returns from 150 to 50 after voiding a +100 credit, i.e. back to its pre-original value; (c) history(cash) still contains the unchanged original transaction (toEqual(original)) plus the new reversal (length 2).",
        "Evaluator built dist/ (npm run build) and independently ran the CLI in an isolated scratch dir with a hand-seeded ledger.json (id 1, cash, type 1/CREDIT, amount 100): `balance cash` -> 100; `void 1` -> printed '#2 voids #1 - cash 0 100'; `balance cash` -> 0; `history cash` showed #1 unchanged (credit 100, balance 100) and #2 (debit 100, voidsId 1, balance 0); ledger.json on disk shows original entry #1 byte-for-byte unchanged and #2 with voidsId: 1.",
        "Evaluator confirmed the Transaction shape in src/types/types.ts matches the contract exactly: `voidsId?: number` added as the last optional field, absent on ordinary transactions and present only on reversals (verified in the seeded ledger.json above -- transaction #1 has no voidsId key at all).",
        "Evaluator independently tested both explicit exclusions and confirmed no speculative handling was added: `void 999` (unknown id) throws an unhandled TypeError (Cannot read properties of undefined (reading 'account')) from src/ledger.ts's `findById(id)!` non-null assertion -- crashes rather than being caught/guarded; calling `void 1` twice in a row (double-void) is not detected or blocked -- it simply appends a second reversal (#3, voidsId 1) with no dedup/guard logic. Both match the contract's 'undefined, not required, not tested' exclusions with zero extra scope creep.",
        "Evaluator reviewed the full diff of src/types/types.ts, src/ledger.ts, src/index.ts, src/ledger.test.ts against HEAD -- Ledger.void() is a thin wrapper around the existing record() (append-only, original never mutated or removed, consistent with CONVENTIONS.md's 'never mutate a saved transaction'), and `void <id>` is wired into the CLI switch in src/index.ts with a corresponding usage-string update."
      ],
      "notes": "Sprint contract: harness/sprint-contracts/ledger-002-void.md. Evaluated and accepted by the evaluator agent: implementation matches the contract's data shape (voidsId?: number, optional, only on reversals), required behavior (flipped type, same amount/account, voidsId, dated today, original untouched), and both explicit exclusions (unknown id, double-void) were left genuinely unhandled rather than given speculative guards. All verification standards from the contract were independently re-run and confirmed by the evaluator, not just trusted from the implementer's notes. Pre-existing, unrelated bug carried over from ledger-001 (the `record` CLI command doesn't parse 'credit'/'debit' strings into TransactionType, only raw numeric enum values) is still present -- out of scope for this feature."
    },
    {
      "id": "ledger-003",
      "priority": 3,
      "area": "ledger",
      "title": "Transfer between accounts",
      "user_visible_behavior": "A user runs `transfer <source> <destination> <amount>` and two atomic transactions are recorded: a debit on the source and a credit on the destination, or neither if it fails.",
      "status": "passing",
      "verification": [
        "Run `transfer cash savings 50`.",
        "Verify cash's balance dropped by 50 and savings' balance rose by 50.",
        "Simulate a failure halfway through and verify neither transaction is left half-recorded."
      ],
      "evidence": [
        "Evaluator independently ran npm run check && npm test on the implementer's uncommitted working tree -> tsc --noEmit clean, 13/13 tests pass (matches implementer's claim; includes the 4 new transfer tests at src/ledger.test.ts:104-159).",
        "Evaluator ran rm -rf dist && ./init.sh from clean -> npm install, npm run check && npm test (13/13 pass), npm run build all succeeded end-to-end.",
        "Evaluator read src/ledger.test.ts:104-159 and confirmed the 4 new tests genuinely cover the contract: (a) balanceOf(cash) drops 100->50 and balanceOf(savings) rises 20->70 after transfer(cash,savings,50); (b) TransferResult.debit.transferId === debit.id and credit.transferId === debit.id (shared value); (c) ledger.json on disk (read via fs.readFileSync, not the in-memory return) contains both the debit and credit ids after one transfer() call; (d) pre-existing transactions (first, second) are byte-for-byte toEqual-unchanged and remain at their original array positions (onDisk[0], onDisk[1]) after transfer().",
        "Evaluator read src/ledger.ts transfer(): confirms exactly one this.load() and one this.save(list) for the whole operation, both legs appended to the same in-memory LinkedList before the single save call, record() never called from within transfer() (avoids a second load/save cycle). debit gets id = list.size()+1 (assigned before append), credit gets the next list.size()+1 after the debit is appended -- i.e. lower id for debit, next sequential id for credit, both carrying transferId === debit.id. Consistent with the existing id-assignment convention already used by record().",
        "Evaluator confirmed src/types/types.ts diff exactly matches the contract: transferId?: number added as the last optional field on Transaction; new TransferResult interface ({ debit: Transaction; credit: Transaction }) added to types.ts (not inlined), per CONVENTIONS.md.",
        "Evaluator confirmed transfer <source> <destination> <amount> is wired into the CLI switch in src/index.ts with a corresponding usage-string update.",
        "Evaluator built dist/ and independently ran the CLI in an isolated scratch dir (outside the repo) with a hand-seeded ledger.json (id 1 cash credit 200, id 2 savings credit 30): balance cash -> 200, balance savings -> 30; transfer cash savings 50 -> printed \"#3 cash -> #4 savings - 50 (transferId 3)\"; balance cash -> 150, balance savings -> 80; history cash showed #1 unchanged (balance 200) plus #3 Transfer to savings (balance 150); history savings showed #2 unchanged (balance 30) plus #4 Transfer from cash (balance 80); ledger.json on disk after the single transfer() call contained all 4 entries written together, #1 and #2 untouched, #3 (type 0/DEBIT) and #4 (type 1/CREDIT) both carrying transferId: 3, confirming the debit (lower id) owns the shared transferId.",
        "Evaluator confirmed the sprint contract's exclusions were left genuinely unhandled with no speculative guards: reviewed the transfer() implementation and found no same-account check, no zero/negative amount check, and no insufficient-funds check -- matching the contract's explicit exclusions."
      ],
      "notes": "Sprint contract: harness/sprint-contracts/ledger-003-transfer.md. Evaluated and accepted by the evaluator agent: implementation matches the contract's data shapes (transferId?: number on Transaction, TransferResult interface in types.ts), the single load/save atomicity requirement (verified by code inspection: one load(), one save(), no internal record() calls), correct id assignment (debit gets the lower id, credit the next sequential id, both carrying transferId === debit.id), and append-only behavior (pre-existing transactions verified byte-for-byte unchanged and unreordered on disk). All contract exclusions (same-account transfer, zero/negative amount, insufficient funds, filesystem-level crash mid-write) were left genuinely unhandled, matching scope. All verification standards were independently re-run by the evaluator (npm run check && npm test, rm -rf dist && ./init.sh, and a manual CLI run in an isolated scratch dir with a hand-seeded ledger.json), not just trusted from the implementer's notes."
    }
  ]
}
